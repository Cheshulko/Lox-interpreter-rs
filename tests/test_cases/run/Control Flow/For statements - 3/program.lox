// This program uses a for loop to print the numbers from 0 to 2
// The loop initializer is ignored in this loop
var hello = 0;
for (; hello < 2; hello = hello + 1) print hello;

// This program uses a for loop to print the numbers from 0 to 2
// The loop increment clause is ignored in this loop
for (var baz = 0; baz < 2;) {
  print baz;
  baz = baz + 1;
}