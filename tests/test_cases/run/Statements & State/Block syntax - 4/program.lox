// This program tests that scopes are properly closed
// Here the inner scope is missing a closing curly brace
// Which leads to a compilation error
{
    var world = 15;
    var foo = 15;
    {
        print world + foo;
}